@if (isset($header))
    <header class="pl-0 lg:pl-64 bg-white dark:bg-gray-800 shadow">
        <div class="mx-auto py-4 px-4 sm:px-6 lg:px-8">
            {{ $header }}
        </div>
    </header>
@endif
